{
  "name": "streamforge-transcoder-worker",
  "version": "1.0.0",
  "description": "Event-Driven FFmpeg Transcoder Worker on AWS EKS Spot Pods (KEDA Scaled)",
  "main": "src/worker.js",
  "scripts": {
    "start": "node src/worker.js",
    "dev": "node --watch src/worker.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "dotenv": "^16.4.5",
    "fluent-ffmpeg": "^2.1.3",
    "uuid": "^10.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
